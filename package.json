{
  "name": "recipes",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run hugo:build",
    "clean": "npm run hugo:clean",
    "serve": "npm run hugo:serve",
    "hugo:build": "hugo -d build",
    "hugo:serve": "hugo server",
    "hugo:clean": "rm -rf build resources public",
    "no-gps-in-images": "node no-gps-in-images.mjs"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "exif": "^0.6.0",
    "glob": "^10.0.0",
    "hugo-extended": "~0.117.0"
  },
  "dependencies": {
    "bootstrap": "^5.2.3",
    "date-fns": "^2.30.0"
  }
}
